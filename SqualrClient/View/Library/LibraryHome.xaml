<UserControl
    x:Class="SqualrClient.View.Browse.Library.LibraryHome"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:browse="clr-namespace:SqualrClient.View.Browse"
    xmlns:browseViewModel="clr-namespace:SqualrClient.Source.Browse"
    xmlns:converters="clr-namespace:SqualrCore.Source.Mvvm.Converters;assembly=SqualrCore"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:library="clr-namespace:SqualrClient.View.Browse.Library"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    x:Name="SqualrLibraryHome"
    d:DesignHeight="512"
    d:DesignWidth="480"
    x:ClassModifier="internal"
    DataContext="{Binding BrowseViewModel, Source={StaticResource ViewModelLocator}}"
    mc:Ignorable="d">
    <x:Code>
        <![CDATA[ public LibraryHome() { InitializeComponent(); }]]>
    </x:Code>
    <UserControl.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <converters:BrowsePageToVisibilityConverter x:Key="BrowsePageToVisibilityConverter" />
    </UserControl.Resources>
    <Border>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition />
            </Grid.RowDefinitions>
            <Grid Grid.Row="0" Background="{StaticResource AvalonDock_Expression_BaseColor3}">
                <Grid Visibility="{Binding CurrentPage, Converter={StaticResource BrowsePageToVisibilityConverter}, ConverterParameter={x:Static browseViewModel:BrowsePage.LibraryGameSelect}}">
                    <library:GamesPage />
                </Grid>
                <Grid Visibility="{Binding CurrentPage, Converter={StaticResource BrowsePageToVisibilityConverter}, ConverterParameter={x:Static browseViewModel:BrowsePage.LibrarySelect}}">
                    <library:LibrariesPage />
                </Grid>
            </Grid>
        </Grid>
    </Border>
</UserControl>